'use strict';
define(['utils/Constant'], function (Constant) {

    var DomainController = function ($scope, DomainSvc) {

    };

    return {
        name: 'DomainEditController',
        fn: ['$scope', 'DomainSvc', DomainController]
    };


});
